个人的思路：
1. 这里本质上是 `二进制求和` -> 进位 + 无进位和（加法器） 
2.  从最低位开始计算就好了 
    - 进位= (a + b +carry(这里是更低一位的进位)) / 2
    - 无进位和 = (a + b +carry(这里是更低一位的进位)) % 2
3. 还要 转为字符串 -> 用字符数组来存上面的结果 -> 从低位开始，所以还需要把整个字符数组反转
4. 值得注意的一点：返回数组的长度 = max（len（a）,b） + 2(多一位进位 和 结束符 0%) ，加3是因为 i = len - 1 
 

