1. 判断2的幂次：
   - 可以循环除2，你先对他n % 2， 然后 n = n /2
   - 位运算，因为2的幂次 ，只有一个1， 用n & (n-1) == 0就好了，但是别忘了条件 n > 0


2. 面试题：FreeRTOS任务优先级配置

   "FreeRTOS 中任务优先级数值越大优先级越高，空闲任务优先级为 0。调度采用抢占式（不同优先级）加时间片轮转（同优先级）。设计时按实时性分层：中断触发任务最高，事件响应次之，计算任务居中，界面显示最低。高优先级任务必须有阻塞点。优先级反转用互斥量解决，它提供优先级继承机制。"