1. 个人的思路就是：有一个虚拟头节点，然后比较大小，接过去就好了，但是最终可能还是剩，直接判断谁剩就接下去就好了
   - 可能有的一个问题就是你返回不了头节点，不对，dummy你是知道的直接返回dummy就好了？

嵌入式面试题：malloc和free是如何工作的？要注意什么？
**malloc通过空闲链表管理堆内存，分配时查找并分割空闲块，free时释放并合并相邻块；嵌入式使用中必须注意内存碎片、分配失败、多线程/中断安全及忘记free导致的泄漏，建议优先使用内存池替代。**