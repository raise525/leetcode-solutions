同day45-232，

今天用C语言来写：栈A来入队，出队时，只需要把A倒入B中，取B的栈顶元素即可

以下分析仅满足添加 N 个元素并删除 N 个元素，即栈初始和结束状态下都为空的情况。

- 时间复杂度： push(), empty() 函数的时间复杂度为 O(1) ；peek() , pop() 函数在 N 次队首元素删除操作中总共需完成 N 个元素的倒序，均摊时间复杂度为 O(1) 。
- 空间复杂度 O(N) ： 最差情况下，栈 A 和 B 共保存 N 个元素。